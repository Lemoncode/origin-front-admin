import React from 'react';
import { AppLayout } from 'layouts';
import { TimeContainer } from 'pods/time';

export const TimeScene: React.FC = () => {
  return (
    <AppLayout>
      <TimeContainer />
    </AppLayout>
  );
};
